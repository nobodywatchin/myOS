# Shared tenant runtime settings. These values are baked into rendered
# Quadlets; rerun tenant-quadlet-install --force after changing image refs.
TENANT_NAME=
PUBLIC_HOSTNAME=
MODEL_ENDPOINT=http://host.containers.internal:18080
AGENT_IMAGE=registry.example.invalid/openclaw/agent-zone:stable
TZ=UTC
