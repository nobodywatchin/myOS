# Zone C storage and secrets

Use Zone C for durable tenant data and secrets.

- `zone-c/storage/` is for durable application data.
- `zone-c/secrets/` is for env files and credentials.
- Mount Zone C into Zone A and Zone B only where strictly required.
