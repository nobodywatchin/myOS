# cosmic/shared/

This tree contains COSMIC-family workstation payloads.

It owns COSMIC-specific greeter and session behavior plus the family marker at `usr/share/myos/workstation/desktop.env` consumed by the shared workstation display-manager helper.

Keep COSMIC-only session glue here instead of mixing it into `files/workstation/shared/`.
