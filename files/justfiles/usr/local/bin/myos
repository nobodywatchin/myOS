#!/bin/sh
# Legacy compatibility alias for the Current CLI.
set -eu

if [ -x /usr/local/bin/current ]; then
  exec /usr/local/bin/current "$@"
fi

if command -v current >/dev/null 2>&1; then
  exec current "$@"
fi

echo "myos: Current CLI compatibility target not found: current" >&2
exit 1
