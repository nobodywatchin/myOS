[Service]
# After BlueBuild reconciles the system Flatpak configuration, keep the
# managed system remote hidden from app/source enumeration while still using
# it for runtime dependencies, then warm common runtime extensions for the
# installed system app/runtime set.
ExecStartPost=-/usr/libexec/current-flatpak-system-maintenance ensure
