# gnome/alma10/

This tree carries Alma 10-only GNOME dconf overrides.

Current policy:

- keep shared GNOME defaults in `files/gnome/shared/`
- use this tree only for Alma 10 GNOME settings that genuinely diverge from the shared baseline
- prefer shared GNOME files when a setting is safe across both GNOME versions
