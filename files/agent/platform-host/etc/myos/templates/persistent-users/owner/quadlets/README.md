# Owner-only Quadlets

Place admin-managed owner-only Quadlets here. myOS installs them only for the
selected owner user and keeps them separate from the baseline role state.

If a service should be available to every enrolled user, move it to the
`baseline/quadlets/` bucket instead.
