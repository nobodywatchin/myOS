---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: gnome-alma9-nvidia-legacy
description: myOS AlmaLinux 9 GNOME image for older-GPU AI work layered on the published full proprietary NVIDIA 580 core image with GNOME NVIDIA extras.
base-image: ghcr.io/myos-dev/core-full-alma9-nvidia-legacy
# GNOME images intentionally track the latest published build of the
# matching myOS core line rather than rebuilding the full stack inline.
image-version: latest
modules:
  - from-file: layers/shared/gnome-base.yml
  - from-file: layers/alma9/gnome.yml
  - from-file: layers/shared/nvidia-gnome.yml
  - type: initramfs
  - type: signing
