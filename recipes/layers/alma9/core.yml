---
modules:
  - type: containerfile
    snippets:
      - RUN ln -s /usr/bin/dnf /usr/bin/dnf4 || true

  - type: dnf
    install:
      packages:
        - mlocate

  - type: script
    scripts:
      - just-el9.sh
