---
# Shared Kubernetes CLI composition reused by the published server/admin images.
modules:
  - type: dnf
    repos:
      files:
        - kubernetes.repo
    install:
      packages:
        - kubectl
