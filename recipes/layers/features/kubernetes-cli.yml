---
# Shared Kubernetes CLI composition reused by the published full-core images.
modules:
  - type: dnf
    repos:
      files:
        - kubernetes.repo
    install:
      packages:
        - kubectl