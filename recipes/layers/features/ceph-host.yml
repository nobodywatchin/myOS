---
modules:
  # Shared server-lane Ceph host prerequisites only. The image does not touch,
  # partition, or mount raw data disks; cluster-time storage orchestration stays
  # with the in-cluster operator.
  - type: files
    files:
      - source: ceph-host/shared
        destination: /
