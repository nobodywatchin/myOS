modules:
  - type: containerfile
    snippets:
      - >-
        RUN test -x /run/rpm-kmod-nvidia-open-modules || (
        mkdir -p /run &&
        printf '%s\n' '#!/bin/sh' 'exit 0' > /run/rpm-kmod-nvidia-open-modules &&
        chmod 0755 /run/rpm-kmod-nvidia-open-modules
        )