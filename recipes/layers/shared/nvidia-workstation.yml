---
modules:
  - type: dnf
    install:
      packages:
        - libnvidia-cfg
        - libnvidia-fbc
        - nvidia-libXNVCtrl
        - nvidia-settings

  - type: kargs
    kargs:
      - nvidia-drm.modeset=1
      - nvidia-drm.fbdev=1
