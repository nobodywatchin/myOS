modules:
  # Shared modern GNOME app delta used by the Alma 10 and Fedora lanes.
  - type: dnf
    install:
      install-weak-deps: false
      packages:
        - baobab
        - gnome-text-editor
        - loupe
        - ptyxis

  - type: gnome-extensions
    install:
      - 7535 # Accent Icons
    #  - 6051 # Quick Web Search
