---
modules:
  - type: dnf
    install:
      packages:
        - almalinux-release-nvidia-driver
  - type: dnf
    install:
      packages:
        - nvidia-open-kmod
        - nvidia-driver
        - nvidia-driver-cuda